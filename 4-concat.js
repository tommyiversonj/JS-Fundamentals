// Get the first and second arguments passed by the user
const firstArg = process.argv[2];
const secondArg = process.argv[3];

// Print the sentence in the required format: "<firstArg> is <secondArg>"
console.log(`${firstArg} is ${secondArg}`);
